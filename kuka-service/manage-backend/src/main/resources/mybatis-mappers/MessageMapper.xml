<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.camelot.kuka.backend.dao.MessageDao">

	<resultMap id="BaseResultMap" type="com.camelot.kuka.backend.model.Message" >
		<result column="id" property="id" />
		<result column="title" property="title" />
		<result column="message" property="message" />
		<result column="type" property="type" typeHandler="com.camelot.kuka.common.mybatis.handler.EnumTypeHandler"/>
		<result column="source_id" property="sourceId" />
		<result column="status" property="status" typeHandler="com.camelot.kuka.common.mybatis.handler.EnumTypeHandler"/>
		<result column="create_time" property="createTime" />
		<result column="create_by" property="createBy" />
		<result column="update_time" property="updateTime" />
		<result column="update_by" property="updateBy" />
	</resultMap>

	<sql id="Base_Column_List">
                id,
                title,
                message,
                type,
                source_id,
                status,
                create_time,
                create_by,
                update_time,
                update_by
    </sql>

	<select id="findList" resultMap="BaseResultMap">
		SELECT
			<include refid="Base_Column_List" />
		FROM
			t_message
		WHERE
			1 = 1
		<if test ='null != entity.status'>
			and status = #{entity.status, typeHandler=com.camelot.kuka.common.mybatis.handler.EnumTypeHandler}
		</if>
	</select>

	<insert id="addBatch">
		INSERT INTO
			t_message(
				id,
				title,
				message,
				type,
				source_id,
				status,
				create_time,
				create_by,
				update_time,
				update_by
		)
		VALUES
		<foreach collection="list" item="item" index="index" separator=",">
			(
				#{item.id},
				#{item.title},
				#{item.message},
				#{item.type, typeHandler=com.camelot.kuka.common.mybatis.handler.EnumTypeHandler},
				#{item.sourceId},
				#{item.status, typeHandler=com.camelot.kuka.common.mybatis.handler.EnumTypeHandler},
				#{item.createTime},
				#{item.createBy},
				#{item.updateTime},
				#{item.updateBy}
			)
		</foreach>
	</insert>
</mapper>